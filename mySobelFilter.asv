function [M,t] = mySobelFilter(I)
x=[-1 0 1;-2 0 2; -1 0 1 ];
y=x';
[n,m]=size(I);
M = zeros(n,m);
G=myGaussianFilter(I, 2);
Ix=myImageFilter(G,x);
Iy=myImageFilter(G,y);
M = sqrt( Ix.*Ix + Iy.*Iy);
t = atan2(Iy,Ix);
